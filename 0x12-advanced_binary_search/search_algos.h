#ifndef SEARCH_ALGOS
#define SEARCH_ALGOS

/* Header file required to use printf */
#include <stdio.h>

/* Function prototypes */
void print_array(int *array, size_t size);
int binary_sub(int *a, size_t s, int v, size_t idx);
int advanced_binary(int *array, size_t size, int value);

#endif
